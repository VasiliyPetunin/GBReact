import React from 'react';
import { ProfileForm } from './ProfileForm';

const Profile = () => {
    return (
        <div>
            <p>PROFILE PAGE</p>
            <ProfileForm />
        </div>
    );
};

export default Profile;